# Copy to .env and fill in real values. Never commit .env.

# Bearer secret for the pricing-estimate endpoint (Authorization: Bearer <SECRET>).
# Required by the API contract (Appendix A). Validated with constant-time comparison.
GAUNTLET_PRICING_SECRET=

# Vast.ai API key — GPU rental for training/hosting open-source models.
VAST_API_KEY=
